export function getAvatarUrl(name?: string): string | undefined {
  if (!name) return undefined
  // Add your own avatar mappings here
  // Example: if (key === "john doe") return "/avatars/john.jpg"
  return undefined
}
